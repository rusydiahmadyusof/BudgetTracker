/**
 * PostCSS Configuration
 * 
 * Purpose: Configure PostCSS for TailwindCSS processing
 * 
 * Free Tools Used:
 * - PostCSS: Free, open-source CSS processor
 * - Autoprefixer: Free, adds vendor prefixes automatically
 * - TailwindCSS: Free CSS framework
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

