/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js configuration
  // This is a free, open-source framework - no license costs
  reactStrictMode: true,
}

module.exports = nextConfig

